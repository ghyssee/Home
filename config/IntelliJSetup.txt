Maven:

View > Tool Windows > Maven Project

To configure the Maven Integration settings

    On the toolbar of the Maven Projects tool window, click settings button for the current project.
    In the Maven page of the Settings dialog, specify the desired options. Refer to the dialog description for details.
	
	Maven Home Directory:
	C:/Post/dev/apache-maven-3.0.5
	
    Apply changes and close the dialog.
	
Do LifeCycle > Compile to resolve Library problems

Global git exludesfile:

Open Git Shell:
git config --global core.excludesfile "C:\Projects\GitHub\Home\global.gitignore"

git config --global http.proxy http://ghyssee:Gizmo201603@proxy.post.bpgnet.net:8080

git config --global --get http.proxy

git config --global --unset http.proxy

git config --global http.proxy http://127.0.0.1:3129

git config --global http.proxy http://localhost:3128

git remote add Home c:/Projects/GitHub/Home

Aanmaken Referentie punt Remote Branch (1-malige actie):
git remote add -m Home RemoteHome https://github.com/ghyssee/Home.git

Verwijderen Remote Branch
git remote delete RemoteHome
(=> verwijdert niet de source op de remote server)

Check:
git remote -v
RemoteHome      https://github.com/ghyssee/Home.git (fetch)
RemoteHome      https://github.com/ghyssee/Home.git (push)

Get Content Of Remote
git pull RemoteHome master

Push the local content to the remote server
git push RemoteHome master

Set The Default repository for Git Shell:
    Right click on Git Shell
    Choose Properties
    Move to the end of the Target field
    Change --open-shell to --open-shell=c:\Projects\GitHub\Home

